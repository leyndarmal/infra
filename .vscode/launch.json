{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch index",
            "program": "${workspaceFolder}/index.js",
            "outputCapture": "std"
        }
    ]
    
}